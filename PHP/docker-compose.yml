services:
  app:
    image: composer:2
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/tmp/cache
    ports:
      - "8080:8080"
    command: sh -c "[ -f .env ] || cp .env.example .env && composer install --no-interaction && php -S 0.0.0.0:8080"

volumes:
  composer-cache:
